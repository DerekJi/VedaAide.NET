name: Build, Test & Deploy

on:
  push:
    branches: [main, features/stage7]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: derekji/vedaaide-api

jobs:
  # ── Build & Test ─────────────────────────────────────────────────────────────
  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore -c Release

      - name: Test
        run: dotnet test --no-build -c Release --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: '**/test-results.trx'

  # ── Docker Build & Push ───────────────────────────────────────────────────────
  publish-image:
    name: Publish Docker Image
    needs: build-test
    runs-on: ubuntu-latest
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/features/stage7') && github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    outputs:
      image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}
      image-digest: ${{ steps.push.outputs.digest }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Build and push
        id: push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/Veda.Api/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Deploy to Azure Container Apps ────────────────────────────────────────────
  deploy:
    name: Deploy to Azure Container Apps
    needs: publish-image
    runs-on: ubuntu-latest
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/features/stage7') && github.event_name == 'push'
    environment: production
    permissions:
      id-token: write   # required for OIDC Azure login
      contents: read
      packages: read    # required to pass GHCR pull secret

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (Federated Identity)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set single revision mode (pre-deploy)
        continue-on-error: true
        run: |
          az extension add --name containerapp --upgrade --only-show-errors -y || true
          az containerapp revision set-mode \
            --mode single \
            --name ${{ vars.CONTAINER_APP_NAME }} \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }} \
            --only-show-errors

      - name: Deploy to Container Apps
        uses: azure/container-apps-deploy-action@v1
        with:
          resourceGroup: ${{ vars.AZURE_RESOURCE_GROUP }}
          containerAppName: ${{ vars.CONTAINER_APP_NAME }}
          imageToDeploy: ${{ needs.publish-image.outputs.image-ref }}
          registryUrl: ${{ env.REGISTRY }}
          registryUsername: ${{ github.actor }}
          # Use long-lived GHCR_PAT when available; GitHub token expires after
          # the workflow run and will cause the Container App to become Unhealthy
          # on the next restart. Set GHCR_PAT secret in repo Settings → Secrets.
          registryPassword: ${{ secrets.GHCR_PAT || github.token }}

